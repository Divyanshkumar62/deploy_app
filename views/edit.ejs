<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a class="bg-blue-600 rounded-md px-3 py-2 text-sm" href="/profile">View Posts</a>
       
        <h5 class="mt-5 mb-5">Update your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind ?" class=" block outline-none w-1/3 p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input class="p-3 bg-yellow-600 block rounded-md mt-2 text-sm" type="submit" value="Update Post">
        </form>

        
    </div>

</body>
</html>